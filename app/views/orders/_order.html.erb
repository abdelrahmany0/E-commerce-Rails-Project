<tr>
  <td scope="row"><%= order.id %></td>
  <td><%= order.user.email %></td>
  <td><%= number_to_currency order.custom_total_price(current_user) %></td>
  <td><%= order.status %></td>
  <td><%= link_to "Show", order_url(order.id), :class => "btn btn-success" %></td>
</tr>